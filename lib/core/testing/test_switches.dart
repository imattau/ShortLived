class TestSwitches {
  /// When true, VideoPlayerView will not create controllers or call play.
  static bool disableVideo = false;

  /// When true, HomeFeed/Repositories won’t create sockets or hit relays.
  static bool disableRelays = false;
}
