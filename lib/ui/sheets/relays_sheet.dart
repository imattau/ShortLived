import 'package:flutter/material.dart';

class RelaysSheet extends StatelessWidget {
  const RelaysSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Relays Sheet'));
  }
}
