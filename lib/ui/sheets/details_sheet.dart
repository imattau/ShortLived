import 'package:flutter/material.dart';

class DetailsSheet extends StatelessWidget {
  const DetailsSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Details Sheet'));
  }
}
